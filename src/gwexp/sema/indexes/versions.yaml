# GENERATED FILE - DO NOT EDIT
# Generated from definitions/registry.yaml


types:
  channel.config:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  channel.readings:
    latest_version: "002"
    versioning_strategy: "literal"
    versions:
      "002":
        summary: >
          - ChannelId: remove
          - additionalProperties: true -> false

  data.channel.gt:
    latest_version: "003"
    versioning_strategy: "literal"
    versions:
      "003":
        summary: >
          - InPowerMetering: drop (metering is declared by the consuming transactive channel, not here)
          - PowerMeteringConstraint axiom: drop
      "002":
        summary: >
          - Quantity: add
          - TelemetryName: spaceheat.telemetry.name:006 -> 007
          - TelemetryQuantityConsistency axiom: add

  derived.channel.gt:
    latest_version: "002"
    versioning_strategy: "literal"
    versions:
      "002":
        summary: >
          - OutputUnit: gw1.unit 000 -> 001
          - OutputUnit: required
          - OutputQuantity: gw1.quantity 000 -> 001
          - OutputQuantity: add
          - OutputUnitQuantityConsistency axiom: add
      "001":
        summary: >
          - InputChannelNames[]: add
          - EmissionMethod: add
          - Parameters: add (Optional)

  fsm.atomic.report:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: >
          - ActionType: remove
          - Action: scalar -> unconstrained object payload

  fsm.full.report:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: "- AtomicList[]: fsm.atomic.report:000 -> 001"

  glitch:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  gw.adc.waveform:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.channel.gap.stats:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.channel.jump.stats:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.channel.noise.stats:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.experiment.run:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.readings:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw1.tank.temp.calibration:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  gw1.tank.temp.calibration.map:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  gw1.unit.quantity.projection:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  ha1.params:
    latest_version: "006"
    versioning_strategy: "literal"
    versions:
      "006":
        summary: >
          - HpMaxKwEl: add as optional
          - HpMaxKwTh: required -> optional
          - HpTurnOnMinutes: add

  i2c.multichannel.dt.relay.component.gt:
    latest_version: "004"
    versioning_strategy: "literal"
    versions:
      "004":
        summary: >
          - AsyncCaptureDelta: ConfigList[0] example now sets it explicitly (the
            relay.actor.config dependency itself is unchanged from 003: :002)
          - ComponentAttributeClassId (cac UUID) -> DeviceType (gw1.device.type value, pascal.case).
            Context-dependent: the device type lived on the referenced cac, not the component.
      "003":
        summary: "- Add I2cBus"

  i2c.thermistor.channel.config:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version. Unit: drop (redundant; unit and scaling are carried by channel identity). Exponent: drop (redundant; unit and scaling are carried by channel identity). CapturePeriodS / AsyncCapture / AsyncCaptureDelta / PollPeriodMs: drop (capture/report tuning moved to operational-params capture.tuning). SendToDerived: drop (derived routing is computed from DerivedChannel InputChannelNames)."

  i2c.thermistor.reader.component.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version. ComponentAttributeClassId (cac UUID) -> DeviceType (gw1.device.type value, pascal.case) -> dropped (TypeName is the kind; board identity lives on the board component). Bus, AdcAddress, AdcReferenceVolts, SeriesResistanceKOhms dropped (facts on the board's thermistor-interface capability entry); AdcName added, naming that entry in the board record's ThermistorAdcs; DataRateSps added (the per-conversion data rate the reader writes in the ADC config word — a member of the board ADC's SupportedDataRatesSps); BoardComponentId added, anchoring the reader to its scada.board.component.gt. Context-dependent: AdcName and BoardComponentId are derived from the source layout, which the standalone component does not carry."

  layout.lite:
    latest_version: "013"
    versioning_strategy: "literal"
    versions:
      "013":
        summary: >
          - ShNodes: spaceheat.node.gt:301 -> 303
          - DataChannels: data.channel.gt:002 -> 003
          - TankModuleComponents / FlowModuleComponents / I2cRelayComponent: cac-carrying component versions -> cac-free DeviceType versions (pico.tank.module 011->012, sim.pico.tank.module 000->001, pico.flow.module 000->001, i2c.multichannel.dt.relay 003->004). Context-dependent: the embedded components' DeviceType lives on their cac, not the component.
          - SystemMode -> ActuationAuthority x ServiceMode split (Heating -> Active/Heating, Standby -> Standby/Heating, MonitorOnly -> MonitorOnly/Heating; mechanical, not the blocking reason for context).
          - Strategy (free manifold-variant string) -> HardwareLayoutTypeName (left.right.dot): the loaded layout word's TypeName is the family; a format, not an enum of layout words, so this word publishes independently of the layout words' status.
      "012":
        summary: >
          - DerivedChannels[]: derived.channel.gt:000 | 001 -> 001
          - DataChannels[]: data.channel.gt:001 -> 002
          - Ha1Params: ha1.params:004 | 005 | 006 -> ha1.params:006
          - I2cRelayComponent: i2c.multichannel.dt.relay.component.gt:002 -> 003
          - ShNodes[]: spaceheat.node.gt:300 | 301 -> 301

  machine.states:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  pico.flow.module.component.gt:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: >
          - ComponentAttributeClassId (cac UUID) -> DeviceType (gw1.device.type value, pascal.case). Context-dependent: the device type lived on the referenced cac, not the component.
          - FlowMeterType: spaceheat.make.model enum value -> gw1.device.type value (pascal.case); the device-type enum is articulated by the hardware layout.
      "000":
        summary: "Initial Sema version"

  pico.tank.module.component.gt:
    latest_version: "012"
    versioning_strategy: "literal"
    versions:
      "012":
        summary: "- ComponentAttributeClassId (cac UUID) -> DeviceType (gw1.device.type value, pascal.case). Context-dependent: the device type lived on the referenced cac, not the component."
      "011":
        summary: "Baseline runtime version (no prior version tracked)."

  relay.actor.config:
    latest_version: "003"
    versioning_strategy: "literal"
    versions:
      "003":
        summary: >
          - Unit: drop (redundant; unit and scaling are carried by channel identity)
          - Exponent: drop (redundant; unit and scaling are carried by channel identity)
          - CapturePeriodS / AsyncCapture / AsyncCaptureDelta / PollPeriodMs: drop (capture/report tuning moved to operational-params capture.tuning)
      "002":
        summary: >
          - StateType: add (optional on the wire for the version's first two days, 2024-12-31 to 2025-01-01)
          - DeEnergizedState: add
          - EnergizedState: add

  report:
    latest_version: "003"
    versioning_strategy: "literal"
    versions:
      "003":
        summary: "- FsmReportList[]: fsm.full.report:000 -> 001"

  report.event:
    latest_version: "004"
    versioning_strategy: "literal"
    versions:
      "004":
        summary: "- Axioms: reinstate ReportIdentityPropagation, ReportCreatedTimePropagation and ReportSourcePropagation"
      "003":
        summary: >
          - Report: report:002 -> 003
          - Axioms: remove former ReportIdentityPropagation and ReportCreatedTimePropagation

  sim.pico.tank.module.component.gt:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: "- ComponentAttributeClassId (cac UUID) -> DeviceType (gw1.device.type value, pascal.case); SimulatesVersion 011 -> 012. Context-dependent: the device type lived on the referenced cac, not the component. Optional SimLifeS / SimRebootS (positive.int seconds) script the simulated pico's liveness."
      "000":
        summary: "Initial Sema version"

  spaceheat.node.gt:
    latest_version: "303"
    versioning_strategy: "literal"
    versions:
      "303":
        summary: "- ActorClass: gw1.actor.class:013 -> 014 (adds FiveVBoss)"
      "302":
        summary: >
          - ActorClass: gw1.actor.class:011 -> 013 (in-place while staging)
          - InPowerMetering: drop (routing is owned by the consuming transactive declaration, not the node)
          - InPowerMeteringRequiresNameplate axiom: drop
      "301":
        summary: >
          - BoardComponentId: add as Optional
          - ActorClass: gw1.actor.class:009 -> 011

  spaceheat.telemetry.quantity.projection:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

enums:
  change.relay.state:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  fsm.report.type:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  gpm.from.hz.method:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gw1.actor.class:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "014":
        added_values:
          - "FiveVBoss"
      "013":
        added_values:
          - "I2cDacWriter"
          - "HpTwin"
      "011":
        added_values:
          - "I2cThermistorReader"

  gw1.actuation.authority:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gw1.emission.method:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gw1.quantity:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "001":
        added_values:
          - "Time"
      "000": {}

  gw1.seasonal.storage.mode:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gw1.service.mode:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gw1.system.mode:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gw1.unit:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "001":
        added_values:
          - "Seconds"
          - "SecondsX10"
      "000": {}

  hz.calc.method:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "001": {}

  i2c.adc.channel:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  i2c.adc.type:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  log.level:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  relay.closed.or.open:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  relay.energization.state:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  relay.wiring.config:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  spaceheat.make.model:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "007":
        added_values:
          - "SAIER__SENHZG1WA"
          - "DFROBOT__DFR0971_TIMES2"
          - "GRIDWORKS__TANKMODULE3"
          - "GRIDWORKS__GW101"
          - "GRIDWORKS__SCADA_GW108"

  spaceheat.telemetry.name:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "007":
        added_values:
          - "BinaryState"

  spaceheat.unit:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "001": {}

  temp.calc.method:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}
