# Sema seed request for the experiments consumer (gwexp.sema snapshot).
# The two staging experiment words, plus the thermistor-reader lineage the
# ads-noise harness decodes from box layouts (000-003; the fleet's
# 000 records now decode typed — no fallback needed. Formats arrive via
# harness). Formats arrive via dependencies.

initial_targets:
  types:
    gw.experiment.run: {}
    gw.channel.jump.stats: {}
    i2c.thermistor.reader.component.gt:
      versions: ["000", "003"]
